#include <stdio.h>


int main() {
    float a, b, c, radius, x1, x2, d;
    printf("Enter the value of a");
    scanf("%f", &radius);
    printf("Enter the value of b");
    scanf("%f", &radius);
    printf("Enter the value of c");
    scanf("%f", &radius);
    d= (b*b)-(4*a*c);
    x1= -b / (2*a) + (d*);
    x2= b / (2*a);



        
}

